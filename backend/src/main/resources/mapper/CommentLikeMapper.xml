<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicommunity.mapper.CommentLikeMapper">

    <select id="selectLikeCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM t_new_comment_likes
        WHERE comment_id = #{commentId}
          AND user_id = #{userId}
    </select>

    <insert id="insertLike">
        INSERT INTO t_new_comment_likes (
            comment_id,
            user_id,
            create_time
        ) VALUES (
            #{commentId},
            #{userId},
            NOW()
        )
    </insert>

    <delete id="deleteLike">
        DELETE FROM t_new_comment_likes
        WHERE comment_id = #{commentId}
          AND user_id = #{userId}
    </delete>

</mapper>
