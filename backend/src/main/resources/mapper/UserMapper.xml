<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicommunity.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.aicommunity.entity.User">
        <id column="id" property="id"/>
        <result column="employee_id" property="employeeId"/>
        <result column="name" property="name"/>
        <result column="email" property="email"/>
        <result column="password" property="password"/>
        <result column="avatar" property="avatar"/>
        <result column="bio" property="bio"/>
        <result column="department" property="department"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, employee_id, name, email, password, avatar, bio, department, create_time, update_time
    </sql>

    <select id="selectByEmployeeId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE employee_id = #{employeeId}
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.aicommunity.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (employee_id, name, email, password, avatar, bio, department, create_time, update_time)
        VALUES (#{employeeId}, #{name}, #{email}, #{password}, #{avatar}, #{bio}, #{department}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.aicommunity.entity.User">
        UPDATE user
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="email != null">email = #{email},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="bio != null">bio = #{bio},</if>
            <if test="department != null">department = #{department},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

</mapper>
