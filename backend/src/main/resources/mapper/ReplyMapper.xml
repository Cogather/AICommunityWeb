<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicommunity.mapper.ReplyMapper">

    <resultMap id="BaseResultMap" type="com.aicommunity.entity.Reply">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="comment_id" property="commentId" jdbcType="INTEGER"/>
        <result column="user_id" property="userId" jdbcType="VARCHAR"/>
        <result column="reply_to_user_id" property="replyToUserId" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="LONGVARCHAR"/>
        <result column="likes" property="likes" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="selectRepliesByCommentId" resultMap="BaseResultMap">
        SELECT 
            id,
            comment_id,
            user_id,
            reply_to_user_id,
            content,
            likes,
            status,
            create_time,
            update_time
        FROM t_new_post_replies
        WHERE comment_id = #{commentId}
          AND status = '0'
        ORDER BY create_time ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT 
            id,
            comment_id,
            user_id,
            reply_to_user_id,
            content,
            likes,
            status,
            create_time,
            update_time
        FROM t_new_post_replies
        WHERE id = #{id}
          AND status = '0'
    </select>

    <insert id="insertReply" parameterType="com.aicommunity.entity.Reply" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_new_post_replies (
            comment_id,
            user_id,
            reply_to_user_id,
            content,
            likes,
            status,
            create_time,
            update_time
        ) VALUES (
            #{commentId},
            #{userId},
            #{replyToUserId},
            #{content},
            #{likes},
            #{status},
            #{createTime},
            #{updateTime}
        )
    </insert>

    <update id="deleteReply">
        UPDATE t_new_post_replies
        SET status = '1'
        WHERE id = #{id}
    </update>

    <update id="deleteRepliesByCommentId">
        UPDATE t_new_post_replies
        SET status = '1'
        WHERE comment_id = #{commentId}
    </update>

    <select id="countLikes" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM t_new_reply_likes
        WHERE reply_id = #{replyId}
    </select>

</mapper>
