<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicommunity.mapper.ZoneTagMapper">

    <resultMap id="BaseResultMap" type="com.aicommunity.vo.ZoneTagVO">
        <result column="tag_name" property="name" jdbcType="VARCHAR"/>
        <result column="tag_count" property="count" jdbcType="INTEGER"/>
    </resultMap>

    <select id="selectZoneTags" resultMap="BaseResultMap">
        SELECT 
            pt.tag AS tag_name,
            COUNT(DISTINCT p.post_id) AS tag_count
        FROM t_new_posts_tag pt
        INNER JOIN t_new_posts_tag_relation ptr ON pt.id = ptr.tag_id
        INNER JOIN t_new_posts p ON ptr.post_id = p.post_id
        WHERE p.zone_id = #{zoneId}
          AND p.status = '0'
          AND (ptr.deleted IS NULL OR ptr.deleted = 0)
          AND (pt.deleted IS NULL OR pt.deleted = 0)
          <if test="toolId != null">
              AND p.tool_id = #{toolId}
          </if>
        GROUP BY pt.tag
        ORDER BY tag_count DESC
    </select>

</mapper>
