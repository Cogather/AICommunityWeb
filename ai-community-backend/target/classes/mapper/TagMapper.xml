<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicommunity.mapper.TagMapper">

    <resultMap id="BaseResultMap" type="com.aicommunity.entity.Tag">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="zone" property="zone"/>
        <result column="tool_id" property="toolId"/>
        <result column="preset" property="preset"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByNameAndZone" resultMap="BaseResultMap">
        SELECT * FROM tags WHERE name = #{name} AND zone = #{zone}
        <if test="toolId != null">
            AND (tool_id = #{toolId} OR tool_id IS NULL)
        </if>
        LIMIT 1
    </select>

    <select id="selectNamesByPostId" resultType="java.lang.String">
        SELECT t.name FROM tags t
        INNER JOIN post_tags pt ON t.id = pt.tag_id
        WHERE pt.post_id = #{postId}
    </select>

    <select id="selectByZoneAndToolId" resultMap="BaseResultMap">
        SELECT * FROM tags WHERE 1=1
        <if test="zone != null and zone != ''">
            AND zone = #{zone}
        </if>
        <if test="toolId != null">
            AND (tool_id = #{toolId} OR tool_id IS NULL)
        </if>
        ORDER BY preset DESC, create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tags (name, zone, tool_id, preset, create_time)
        VALUES (#{name}, #{zone}, #{toolId}, #{preset}, NOW())
    </insert>

</mapper>
