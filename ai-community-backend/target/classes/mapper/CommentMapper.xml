<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicommunity.mapper.CommentMapper">

    <resultMap id="BaseResultMap" type="com.aicommunity.entity.Comment">
        <id column="id" property="id"/>
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="likes" property="likes"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM comments WHERE id = #{id}
    </select>

    <select id="selectByPostId" resultMap="BaseResultMap">
        SELECT * FROM comments WHERE post_id = #{postId} ORDER BY create_time ASC
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM comments WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="countByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM comments WHERE user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comments (post_id, user_id, content, likes, create_time, update_time)
        VALUES (#{postId}, #{userId}, #{content}, #{likes}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById">
        UPDATE comments
        SET content = #{content},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM comments WHERE id = #{id}
    </delete>

    <delete id="deleteByPostId">
        DELETE FROM comments WHERE post_id = #{postId}
    </delete>

    <update id="incrementLikes">
        UPDATE comments SET likes = likes + 1 WHERE id = #{id}
    </update>

    <update id="decrementLikes">
        UPDATE comments SET likes = GREATEST(likes - 1, 0) WHERE id = #{id}
    </update>

</mapper>
