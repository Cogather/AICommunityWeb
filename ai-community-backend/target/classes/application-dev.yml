server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_community?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root

logging:
  level:
    com.aicommunity: DEBUG
    org.springframework.web: DEBUG
    org.mybatis: DEBUG

# 开发环境CORS配置
cors:
  # 开发环境允许所有源
  allow-all: true
  # 也可以配置具体的前端地址，例如：http://localhost:3000,http://localhost:5173
  allowed-origins:
