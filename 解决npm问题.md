# 解决 npm 在 PowerShell 中无法使用的问题

## ✅ 问题已解决！

你的 Node.js 和 npm 已经正确安装：
- **Node.js 版本**: v24.12.0
- **npm 版本**: 11.6.2
- **npm 镜像源**: 已配置为淘宝镜像

## 🔧 问题原因

PowerShell 的执行策略阻止了 `npm.ps1` 脚本运行，但可以使用 `npm.cmd` 来替代。

## 💡 解决方案

### 方法 1：使用 npm.cmd（推荐）

在 PowerShell 中，使用 `npm.cmd` 代替 `npm`：

```powershell
# 查看版本
npm.cmd -v

# 安装依赖
npm.cmd install

# 运行脚本
npm.cmd run dev
```

### 方法 2：修改 PowerShell 执行策略（永久解决）

以**管理员身份**打开 PowerShell，运行：

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

然后就可以直接使用 `npm` 和 `node` 了。

### 方法 3：使用 CMD 而不是 PowerShell

在 CMD 中，`npm` 和 `node` 可以直接使用，不需要加 `.cmd`。

## 🚀 现在可以启动项目了

在项目目录下运行：

```powershell
npm.cmd run dev
```

或者使用 CMD：

```cmd
npm run dev
```

## 📝 常用命令（使用 npm.cmd）

```powershell
# 查看版本
npm.cmd -v
node.exe -v

# 安装依赖
npm.cmd install

# 启动开发服务器
npm.cmd run dev

# 构建生产版本
npm.cmd run build

# 预览构建结果
npm.cmd run preview
```

## 🎉 项目已准备就绪！

所有依赖已安装完成，现在可以开始开发了！

