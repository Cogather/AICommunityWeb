# 测试环境配置
# 测试环境API地址（请根据实际测试环境修改）
VITE_TEST_API_BASE_URL=https://test-api.example.com/api

# 浏览器标签页标题
VITE_APP_TITLE=AI社区 (测试版)
# Favicon 图标路径 (文件需放在 public/ 目录下)
VITE_APP_FAVICON=/logo.svg

# 或者使用相对路径
# VITE_TEST_API_BASE_URL=/api
